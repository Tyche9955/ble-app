name: ble_car
description: BLE小车控制APP
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: 1.14.0
  permission_handler: ^11.0.0
  provider: ^6.1.2

flutter:
  uses-material-design: true
